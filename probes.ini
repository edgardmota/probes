probe-port = 4001
log-file = /var/log/probes

[boacompra1]

	[[dumps]]
	dir = /opt/cdp-server/backups_manual/boacompra1/mysqldump
	check-interval = daily

	[[master-binlogs-rsync]]
	dir = /opt/cdp-server/backups_manual/boacompra1/mysql-bin-logs
	check-interval = minutely

	[[mysql-replication-test]]
	user = root
	password = L7O0PsQ&Fw
	port = 33061
	check-interval = minutely

[boacompra2]

        [[dumps]]
        dir = /opt/cdp-server/backups_manual/boacompra2/mysqldump
        check-interval = daily

        [[master-binlogs-rsync]]
        dir = /opt/cdp-server/backups_manual/boacompra2/mysql-bin-logs
        check-interval = minutely

        [[mysql-replication-test]]
        user = root
        password = L7O0PsQ&Fw
        port = 33062
        check-interval = minutely

[sites]

        [[dumps]]
        dir = /opt/cdp-server/backups_manual/sites/mysqldump
        check-interval = daily

        [[master-binlogs-rsync]]
        dir = /opt/cdp-server/backups_manual/sites/mysql-binlogs
        check-interval = minutely

        [[mysql-replication-test]]
        user = root
        password = L7O0PsQ&Fw
        port = 33063
        check-interval = minutely

[auth]

        [[dumps]]
        dir = /opt/cdp-server/backups_manual/auth/mysqldump
        check-interval = daily

        [[master-binlogs-rsync]]
        dir = /opt/cdp-server/backups_manual/auth/mysql-bin-logs
        check-interval = hourly

        [[mysql-replication-test]]
        user = root
        password = L7O0PsQ&Fw
        port = 33064
        check-interval = hourly
